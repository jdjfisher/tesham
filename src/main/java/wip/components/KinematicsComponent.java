/*
package wip.components;

import wip.core.AbstractComponent;
import wip.core.Entity;
import maths.vectors.Vector3f;

public class KinematicsComponent extends AbstractComponent
{
    private Vector3f velocity;
    private Vector3f acceleration;

    public KinematicsComponent(Entity parent)
    {
        super(parent);
        velocity = new Vector3f();
        acceleration = new Vector3f();
    }

    public Vector3f getVelocity()
    {
        return velocity;
    }

    public Vector3f getAcceleration()
    {
        return acceleration;
    }
}
*/
