/*
package wip.managers;

import wip.components.TransformComponent;
import wip.core.Entity;
import wip.core.AbstractComponent;
import wip.core.AbstractComponentManager;

public class TransformComponentManager extends AbstractComponentManager<TransformComponent>
{
    @Override
    public void subscribe(Entity entity)
    {
        subscribe(entity, new TransformComponent(entity));
    }

    @Override
    public void update(float interval)
    {

    }
}
*/
